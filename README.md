# Аналитический Telegram-бот для видео-платформы

Бот принимает вопросы на русском языке и возвращает **одно целое число** — результат аналитического запроса к базе данных.  
Примеры корректных вопросов:

- «Сколько всего видео есть в системе?»  
- «Сколько видео у креатора с id 123 вышло с 1 ноября 2025 по 5 ноября 2025 включительно?»  
- «Сколько видео набрало больше 100 000 просмотров за всё время?»  
- «На сколько просмотров в сумме выросли все видео 28 ноября 2025?»  
- «Сколько разных видео получали новые просмотры 27 ноября 2025?»

Бот **не хранит контекст диалога**: каждый запрос обрабатывается независимо.

---

## 📦 Требования

- Python 3.10+
- Docker и [Docker Compose](https://docs.docker.com/compose/install/) (рекомендуется)
- PostgreSQL 15+
- Telegram Bot (через [@BotFather](https://t.me/BotFather))
- ИИ-модель через OpenAI-совместимый API (например, **DeepSeek**)

---

## 🚀 Запуск проекта

### С помощью Docker (рекомендуется)

1. Убедитесь, что установлены:
   - [Docker](https://docs.docker.com/get-docker/)
   - [Docker Compose](https://docs.docker.com/compose/install/)

2. Склонируйте репозиторий:
   ```bash
   git clone <ваш-репозиторий>
   cd <папка-проекта>

3. Создайте файл .env.production в папке fastapi:

    APP_ENV=production

    DEBUG=false

    ### Данные для подключения к базе данных (измените при необходимости)
    DATABASE_URL_ASYNC=postgresql+asyncpg://telegram_bot_ai:telegram_bot_ai!!@postgres:5432/

    ### Токен ТГ бота
    TG_BOT_TOKEN=

    ### ID бота
    TG_BOT_ID=

    ### ID чата для отладки (куда бот будет отправлять ответы)
    TG_CHAT_ID=

    ### Токен для доступа к ИИ-модели
    AI_TOKEN=

    ### URL OpenAI-совместимого API (например, DeepSeek)
    AI_API_URL=https://api.deepseek.com

4. Запустите сервисы:

    docker compose up --build    
    docker compose up -d        

5. Загрузите тестовые данные:

    docker compose exec fastapi python cli/test_load_data.py run

## 🧠 Архитектура бота

### Бот построен по принципу:

Естественный язык → ИИ-парсинг (DeepSeek) → извлечение параметров → шаблон SQL → безопасный запрос → число

### Этапы обработки:

Входящее сообщение поступает от пользователя в Telegram.

Запрос отправляется в ИИ-модель (например, DeepSeek) через OpenAI-совместимый API.

Модель промптом настроена на:

извлечение дат (28 ноября 2025, с 1 по 5 ноября 2025);

определение метрики (количество, сумма);

выделение фильтров (creator_id, просмотры > N, дата публикации).

Результат ИИ-парсинга — структурированный объект (например, JSON с полями type, filters).

Ответ — одно целое число, отправляется пользователю.
